import React from "react"

export default function TestimonialSource({children}) {
    
    
    return(
        <div className="matcha-testimonial-source">
            {children}
        </div>
    )
}